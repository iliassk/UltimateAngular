const coupon: string = 'pizza25';

function normalizeCoupon(code: string): number {
  return 12;
}

const couponMessage: string = `Final coupon is ${normalizeCoupon(coupon)}`;

console.log(couponMessage);
